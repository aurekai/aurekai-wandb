name: validate

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ensure scaffold files exist
        run: |
          test -f README.md
          test -f LICENSE
